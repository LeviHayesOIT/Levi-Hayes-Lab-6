@model TreaterModel

@{
    ViewData["Title"] = "Home Page";
}

<body class="page">
    <form method="post" asp-action="Create" asp-controller="Costume">
        <div class="row">
            <div class="form-group col-sm-4">
                <label asp-for="CostumeName" class="control-label"></label>
                <input asp-for="CostumeName" class="form-control" />
                <span asp-validation-for="CostumeName" class="text-danger"></span>
            </div>
        </div>
        <button type="submit" class="btn btn-default" value="Model">Create</button>
    </form>
    <br />
    <div>
        <h2>Current Treaters</h2>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var costume in Model.CostumeList)
                {
                    <tr>
                        <td>
                            @costume.costume
                        </td>
                        <td>
                            <form method="post" asp-action="Delete" asp-controller="Costume">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@costume.id" />
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</body>
